#include <stdio.h>
#include "writer.h"

int write_output(const char *filename, Chunk *chunks, int chunk_count, size_t original_size) {
    // TODO: Shaheer — write header + compressed chunks to output file
    return 0;
}