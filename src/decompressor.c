#include <stdio.h>
#include <stdlib.h>
#include "decompressor.h"

int decompress_file(const char *input, const char *output) {
    // TODO: Shaheer — read compressed file, decompress each chunk, reconstruct
    return 0;
}